# ── shell ───────────────────────────────────────────────────────────────────

# Read-only persona: no shell tools. The advisor reviews and audits; it never
# executes commands or modifies files (see the ROLE in the persona). Shell rows
# from the `standard` source were removed deliberately.

